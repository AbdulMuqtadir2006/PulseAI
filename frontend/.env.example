# Backend API base URL (no trailing slash). In local dev you can leave this
# unset — Vite's dev proxy forwards /api to http://localhost:8000 instead.
VITE_API_URL=https://your-backend.up.railway.app
